# ---------------------------------------------------------------- #
# MONOREPO ROOT IGNORE
# This file is located in the top-level directory: /gas-app-project
# ---------------------------------------------------------------- #

# 1. Dependency Directories
# Global dependency folders for both frontend and backend
**/node_modules
/backend/node_modules
/frontend/node_modules

# 2. IDE and OS Artifacts
# macOS
.DS_Store
# Windows
Thumbs.db
# VS Code
.vscode/*
!/.vscode/settings.json    # Keep shared settings if needed
!/.vscode/tasks.json
!/.vscode/launch.json
!/.vscode/extensions.json

# 4. Frontend (React/Vite) Build Artifacts
/frontend/.vite/      # Vite cache
/frontend/coverage    # Test results
/frontend/public/build # Common build output if used

# 5. Backend (Node.js/Express) Artifacts
/backend/logs
npm-debug.log*

# 6. Database (Optional)
# Files related to your local MySQL setup (if not using remote DB)
# You might want to ignore any local test data files here.

# 7. Package Lock Files
# You may want to commit one of these, but generally ignore the others.
package-lock.json
npm-shrinkwrap.json
yarn.lock